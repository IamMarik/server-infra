services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    env_file:
      - ${MODULE_ENV_FILE}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - server-infra

volumes:
  caddy-data:
  caddy-config:

networks:
  server-infra:
    name: server-infra
    external: true
