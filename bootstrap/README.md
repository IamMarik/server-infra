# Bootstrap

First-time server preparation.

This area should contain setup steps that make a fresh Ubuntu VPS ready for `server-infra`.

Expected tasks:

- create deploy user
- configure SSH
- install Docker and Docker Compose
- configure firewall
- create `~/projects`
- clone `server-infra`
