# Database

## Purpose

Database infrastructure and backup/restore operations.

## Components

This module is intentionally minimal in v1. Add PostgreSQL/Redis here only when the database should be owned by this infrastructure repository.

Application-owned migrations and schemas belong to application repositories.
