# Target: /etc/server-infra/modules.env
# Required permissions: root:root 0640
# Changes require validation before deployment.

# Required. Ordered, space-separated module names. Sensitive: no.
# Enable only modules that have completed their host migration and preflight.
ENABLED_MODULES="proxy"
